{
  "mcpServers": {
    "security-guardian": {
      "command": "python",
      "args": [
        "${SG_ROOT}/middleware/mcp_proxy.py",
        "--upstream", "npx @modelcontextprotocol/server-filesystem /workspace",
        "--policy", "normal"
      ],
      "env": {
        "SG_AGENT_NAME": "cursor"
      }
    }
  }
}
